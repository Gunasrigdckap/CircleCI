version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Run main.py
          command: python main.py
      - run:
          name: Run tests
          command: python tests.py

workflows:
  build_now:
    jobs:
      - build

